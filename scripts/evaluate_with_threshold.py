def main():
    model_name = ""
    threshold = 25
    setup = {
        "exp_name": f"{model_name}_with_th{threshold}",
        "threshold": threshold,
        "model_name": model_name,
    }


if __name__ == "__main__":
    main()
